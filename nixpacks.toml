[phases.setup]
nixPkgs = ["...", "playwright-driver"]

[phases.build]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "gunicorn -b 0.0.0.0:$PORT main:app --timeout 120"
